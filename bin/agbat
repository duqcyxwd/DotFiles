#!/bin/bash

echo "Preview by agbat"
# Example input data
# t='path/npm.sh:40:  fetchResult'
data=$1

fn=$(echo $data | cut -f 1 -d ":")
line=$(echo $data | cut -f 2 -d ":")
echo "$fn"
echo "$line"
bat --style=numbers --color=always $fn -r $line:$(($line+200))
