#!/bin/zsh

CACHE_DIR=$RUNCACHED_CACHE_DIR/$(kube_cache_key)

# Namepsace with 2 hours ttl
TTL=${1:-7200}

runcached --ignore-pwd --ignore-env --ttl $TTL --cache-dir $CACHE_DIR kubectl get namespaces --sort-by=.metadata.creationTimestamp | awk 'NR == 1; NR > 1 {print $0 | "tac"}'

